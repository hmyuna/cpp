#include <stdio.h>
#include <windows.h>
#include <iostream>
using namespace std;
int main (){
/*	int i, len = 0, k;
	scanf("%d %d", &i, &k);
	for(int j = 0; j < i; j += 1){
		if((j % k) == 0){
			len += 1;
		}
	}
	printf("%d", len); 
	int num;
	long long a;
	cin>>num>>a;
//	cout<<pow(num, a);
*/
	Sleep(1000);
	return 0;
}
